Items that I want/need to work on:

* Make code fully compliant with safety-critical standards (MISRA C2012).
  Current status: zero stdlib dependencies achieved, single-return applied to
  okj_parse().  Full MISRA compliance requires a MISRA-capable toolchain and
  formal analysis; deferred post-MVP.

* Add more tests to check for more possible error conditions and various
  input data.
